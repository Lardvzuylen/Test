# Dataiku code environment (Python 3.10+) voor het Woo-lakproject.
# Voeg deze toe onder: Administration > Code envs > <jouw env> > Packages to install.
# Na installatie ook eenmalig het NL spaCy-model ophalen:
#   python -m spacy download nl_core_news_lg

pymupdf>=1.23
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
SQLAlchemy>=2.0
psycopg2-binary>=2.9
pandas>=1.5

# Alleen nodig voor OCR van gescande PDF's (vereist ook systeem-Tesseract + 'nld' taalpack):
pdf2image>=1.16
pytesseract>=0.3.10
Pillow>=9.0
